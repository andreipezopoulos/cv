## Me, playing around with Nix and Latex
To build my cv just run the following command at project's root directory:

``` sh
nix-build
```

It only needs [Nix](https://nixos.org/download.html#nix-install-linux) installed.
